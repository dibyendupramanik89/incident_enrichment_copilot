# Consolidated requirements for all services
# Install: uv pip install -r requirements.txt

# Web framework & server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# HTTP client (MCP servers, Ollama)
httpx>=0.27.0
requests>=2.31.0

# LLM (OpenAI-compatible — works with Ollama too)
openai>=1.40.0

# Data validation
pydantic>=2.8.0

# Frontend
gradio>=4.0.0

# Utilities
python-dotenv>=1.0.1

# Vector DB
chromadb>=1.5.0

# Testing (dev)
pytest>=8.0.0
pytest-asyncio>=0.23.0
ruff>=0.5.0
